<Window x:Class="SubscriptionOverlayWidget.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Subscription Overlay"
        SizeToContent="WidthAndHeight"
        MinWidth="220"
        MinHeight="110"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent">
    <Border x:Name="OverlayBorder"
            Style="{StaticResource ModernCard}"
            Padding="16"
            CornerRadius="16"
            BorderBrush="{StaticResource BorderBrush}"
            MouseLeftButtonDown="OverlayBorder_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel x:Name="BrandingPanel" Orientation="Horizontal" VerticalAlignment="Center">
                    <Image x:Name="LogoImage" Width="16" Height="16" Margin="0,0,8,0" Stretch="Uniform" VerticalAlignment="Center"/>
                    <TextBlock x:Name="BrandTextBlock" Text="WormSoft" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel x:Name="SubscriptionPanel" Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="Подписка" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextAlignment="Right"/>
                    <TextBlock x:Name="SubscriptionLevelTextBlock" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Text="—" TextAlignment="Right"/>
                </StackPanel>
            </Grid>

            <TextBlock x:Name="LimitTextBlock"
                       Grid.Row="1"
                       Margin="0,14,0,0"
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Text="—"/>

            <StackPanel x:Name="ResetPanel" Grid.Row="2" Margin="0,12,0,0">
                <TextBlock x:Name="ResetTextBlock"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AccentAmberBrush}"
                           Text="Сброс лимитов: —"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
